# Build Queue — the one human input

> **This is the only hand-maintained status file.** /ops reads it to (1) order the
> REDs ("next up" = top-to-bottom here) and (2) flag YELLOWs (currently building).
> Everything else on /ops is derived from real signals.
>
> Format: priority = line order. `- [x]` done · `- [~]` building now · `- [ ]` up next.
> Edit this file on GitHub; /ops picks it up within 5 minutes.

- [x] Section 1 — stamp THE-GOAL.md + lean rules-of-engagement block
- [x] Section 2 — /ops live operations ledger (this dashboard)
- [x] Apply predictions/outcomes SQL to live Supabase + verify a prediction row lands
- [x] fl_dor_sales_tax — run schema migration + first backfill, move registry to active
- [x] Section 3 — plan master synthesizer flesh, starting from /ops state
- [x] Fix US-41 / Tamiami Trail corridor naming collision
- [x] safety-swfl — FBI Crime Data Explorer replaces unfit FIBRS (#59); brain LIVE (bullish, -9.7% YoY), `public.fdle_crime_swfl` backfilled 2022–2024, quarterly cron re-enabled, master v71
- [x] Highlighter Phase 1 — in-page ask layer (`docs/superpowers/specs/2026-06-07-highlighter-in-page-ask-chart-design.md`): fact detection + mobile chips, popup (3 states), `/api/converse` (haiku, metered, enforcement OFF), precomputed suggestions, discovery coachmark
- [x] Highlighter Phase 1.5 — /r/ AI answer-quality pass (`docs/superpowers/plans/2026-06-09-highlighter-iteration.md`): header-badge grounding, 3-lane be-Claude/grounded/offer prompt, slug→label CLEAN fix, FOCUS/NATURAL/no-echo/concise voice, type-aware chips (killed "what's driving <date/token>"), chart/section real-text fix, off-screen shield, SWFL-wide platform framing — all `/r/` pages
- [x] Highlighter Phase 2 — real-time follow-up prompts + selection-type awareness (`docs/superpowers/plans/2026-06-09-highlighter-iteration.md`): `⟦FOLLOWUPS⟧` same-call tail split client-side (`splitFollowupTail`), `deriveSelectionType` (section/token/date/place/metric) passed to converse — directive GATED on selection_type (dock spends no tokens); + mobile number tap-targets (`MetricValueCell` string|number, `DataRow` FactChip) + chip analytics (`recordAsk` → `data_requests.selection_type/is_realtime/from_chip`, columns live). Tests 64✓, tsc/eslint clean. Live runtime verify (model emits tail, chips render, mobile tap) pending → check `highlighter_realtime_prompts` stays open.
- [x] §D2 — Universal Location Search endpoints: `GET /api/where?q=` (any input → dossier, plain text or JSON) + `GET /api/z/[zip]` canonical ZIP permalink
- [x] §D1 — MCP `swfl_fetch` zip fan-out: `resolveLocation` + `assembleLocationDossier` for zip/location params (no explicit `report_id`); pinned non-master `report_id` keeps the single-brain `fetchDetailRow` drill (back-compat). Tier-2 default, capped (true-ZIP + 8 headline); `_meta.dossier` = full `LocationDossier` matching `/api/z?format=json`. 5 integration tests green
- [x] §D3 — search box (any input → redirect) + identity header card + grain chips + did-you-mean; generalized `/r/zip-report/[zip]/page.tsx` off `assembleLocationDossier`. New `/r` index + `/r/search` resolver + shared `lib/location-surface.ts` (19 tests). Also fixed `place-resolver.mts` `import.meta.dirname` impurity that threw on `resolveLocation` in the Next/Vercel bundle (was latent on D2's corridor path too — `/api/where?q=North Naples` now 200). §D COMPLETE
- [x] §E — address geocoder: `refinery/lib/geocode.mts` `geocodeAddress(q)` (Mapbox v6 forward primary + Census fallback; URL-restricted token → explicit `Referer` header, works on Vercel; locality fall-through reverse-geocodes `types=postcode`). Wired into `location-resolver.mts` step 6 with `resolveZip().in_scope` (6-county ⊃ METRO_4) scope gate. + `LocationDossier.coverage_caveats` surfaces the Charlotte/Sarasota housing-only asymmetry as a stated boundary (not silent thinning) in `/api/where`+`/api/z` JSON + MCP `_meta.dossier`. 23+52 tests green. LIVE-VERIFIED 2026-06-10: address→33908, Pelican Bay→34108, Charlotte→33950+caveat, Sarasota→34237; `connector_output_live_verify` + `mcp_zip_fanout_live_verify` CLOSED
- [x] Highlighter — session persistence (thread lifted to `HighlighterProvider`, survives close/reopen, shared popup⇄dock) + briefcase capture tray (`localStorage` draft project, `ProjectItem` spine) + "File this answer/figure/report" + cross-cell snapping w/ ambiguous-mix suppression (Projects S1, `…/session-1-highlighter-thread-briefcase__OPUS/`). Per-brain `/r/` tuning tail folds into later sessions.
- [x] Charts Tier A — deterministic at-a-glance chart from key_metrics/detail_tables (`docs/superpowers/specs/2026-06-07-chart-generation-three-tier-design.md`); wire the dead `Dossier.chart` slot + new `/r/` render child
- [x] Charts Tier B + Highlighter "Chart this" — build `buildChartForIntent` (routeChart has NO consumer today) + `HBarChart` responsive fix (shared sub-task)
- [x] `/c/[id]` saved chart → `/project/[id]` (first `auth.uid()` RLS policy) → PDF via `window.print()` — Projects + Briefcase S0–S5 (`docs/superpowers/plans/2026-06-10-projects-briefcase-assembly/`)
- [x] Assembly engine + hosted deliverable — `POST /api/projects/[id]/build` (forced-tool Sonnet, narrative-only) + `/p/[id]` public page (source + as-of under every exhibit) + EXACT-anchor moat lint (regenerate-then-strip, adversarially hardened) + deterministic jargon scrub + restyle-without-re-LLM (S6, `…/session-6-assembly-engine__OPUS/`). 71 deliverable tests green, tsc clean. Built LOCALLY (8 commits) — awaiting operator diff-review of the build route + system prompt, then push. Live 4-template build (<8s) + `deliverable_anchor_lint` close are post-deploy. S7's `aaf7a10` revoke route now has its `/p/[id]` dependency.
- [x] Delivery + revoke — email draft / share-sheet / mailto; `/p/[id]` 410 on revoke; owner kill switch (S7, `0409888`)
- [x] Uploads v1 — `project-uploads` Storage bucket + RLS path-prefix; attach+caption (10 MB/file, 10/project); cross-user read DENIED (S8). Bucket+RLS LIVE in prod; `storage_rls_scope_verify` CLOSED on a live two-account test (8/8: cross-read/sign/write + anon + expiry all DENIED, owner-reads-own positive control). UI built LOCALLY — awaiting operator diff-review + push, then deploy.
- [x] Presentation Deliverable Engine (`docs/superpowers/plans/2026-06-10-presentation-deliverable-engine/`) — Phase 0–6 ALL ✅ deployed (`4be4405`/`f6d9a02`/`69e076b` on main). Phase 6 (brand theming): `lib/deliverable/brand-theme.ts` (`extractBrandTheme`/`toChartTheme`); theme injected at render time in `/p/[id]`; `FrameRenderer` CSS custom props (`--chart-primary`/`--chart-accent`); logo in branding slot; 136 deliverable tests, tsc 0.
- [x] Free ZIP investor composite — **ZHVI ingest** (`ingest/{duckdb_pipelines,pipelines}/zhvi_swfl/**`, Tier-1→Tier-2 clone of ZORI; cadence + GHA day 22/23; verified live 33,922 rows/109 ZIPs) + **home-values-swfl** (per-ZIP `home_values_by_zip`) + **investor-zip-swfl** (value+rent+flood joined by exact key → gross rent yield → flood-adjusted cap rate, in code; STR null + ODD `airdna_str_swfl` parked). Fixed pre-existing `rentals-swfl` smoothing-lint time-bomb. 44 tests; vocab `--all` 30 brains. ⬜ **deploy:** run zhvi-tier1/2 workflows + `GRANT SELECT … data_lake` → real `.md`s on first nightly.
- [x] Fixture-frames live (L0–L3): L0 binder seam ✅ · L1 storm-timeline ✅ · L2 franchise-survival ✅ · L3 seasonal-radial ✅ — all local, push pending
- [x] Email Digest Phase 1 (`docs/superpowers/plans/2026-06-11-email-digest-phase1.md`) — ✅ @react-email + types.ts + log-io.mts + GHA cron; ✅ DigestEmail.tsx (Task 4, themeable via `BrandTheme`/white-label logo slot, 7 sections) + build-digest.mts (Task 6, delta floors/idempotency/RFC 8058); ✅ **fetch-digest-data.mts rebuilt vs the REAL brain schema** (was hallucinated → empty digest: brace-match OUTPUT parse, cell-based ZIP rows via `row.key`+`cells`, `housing_`-prefixed county slugs, markdown-table city voices); ✅ **city-voice curation** (EMAIL.md Rule 2.5: market-relevant ranking + dedup + `SUBJECT_TOPICS` allowlist so breaking never leads the subject + data-lede fallback `33908 DOM hits 87 | SWFL Data Gulf`); ✅ DIGEST_SENDER_NAME/ADDRESS/CONTACT set + Resend domain verified + 3-key strategy (digest-cron/waitlist-web=sending_access, full_access=Phase 2 Audiences). 32 email tests green; live dry-run renders 20KB from real data. ⬜ **runtime verify only:** first automated cron send (Mon–Fri 10:00 UTC) delivers + logs `send_status=sent` (check `digest_cron_first_send_verify`).
- [~] Email Digest Phase 2 — subscriber list (`docs/email-marketing/README.md` Phase 2). ✅ **capture path BUILT + live-verified 2026-06-12** (separate list from `waitlist`; Audiences→**Segments** SDK migration verified live): `public.email_subscribers` table applied; `lib/email/marketing-client.ts` (full_access client + `getDigestSegmentId`); `scripts/email/setup-digest-segment.mts` ran live (segment created); `app/api/email/subscribe` (idempotent contact-create → DB mirror, e2e 200 against real Resend+Supabase, test rows cleaned); `components/email/DigestSubscribe.tsx` wired into landing Footer + every `/r/[slug]`; `app/api/email/broadcast` (bearer, draft-by-default, rejects HTML w/o `{{{RESEND_UNSUBSCRIBE_URL}}}`). 8 new validation tests (40 email total) + tsc/eslint clean. ⬜ **go-live flip:** Vercel env (`RESEND_AUDIENCES_KEY`/`RESEND_DIGEST_SEGMENT_ID`/`DIGEST_BROADCAST_SECRET`) + redeploy; **CAN-SPAM address swap (hard gate)**; wire cron → broadcast (footer unsub token + POST to `/api/email/broadcast`).
- [~] Multi-tenant email — **Unit F cron scheduler** (`docs/superpowers/plans/2026-06-12-email-product-multitenant/`, Wave 2 capstone) BUILT + pushed (`63dbbf1`/`982c3d3`/`616dd3b`): `lib/email/scheduler.ts` (pure DI core) + `scripts/email/run-schedules.mts` (claim via `claim_due_email_schedules` RPC `FOR UPDATE SKIP LOCKED`; imported `computeNextRunAt` cadence/DST; usage-gate skip+notify; unsubscribe-token inject+assert; crash-orphan reaper; 30s broadcast timeout; DRY_RUN read-only) + `.github/workflows/email-scheduler.yml` (`*/15`). 149 `lib/email` tests; spec+code-quality reviewed. ⬜ **go-live:** apply `docs/sql/20260612_email_schedule_claim_fn.sql` (direct PG — was firewalled in build session) + set GHA secret `DIGEST_BROADCAST_SECRET` (missing; matches Vercel) + first live DRY_RUN→real verify. Check `email_scheduler_f_live_verify`.
- [x] Welcome chat → recurring-email hook (copy only, `233fc06`): `WELCOME_SYSTEM` + 4 PROMPTS + footer now lead with "auto-email fresh data to YOUR clients", not "sign up and you can build it"; route test re-pointed off the old funnel onto the no-invention + auto-email hook; 4/4 green.
- [ ] Email funnel "the rest" (`docs/superpowers/plans/2026-06-13-email-funnel-the-rest/`) — 5 grounded tasks closing the gap from the built-but-OFF multi-tenant engine to the welcome promise: 01 `scope jsonb` column + parser capture (safe-additive) · 02 scoped content via the grounded engine · 03 worker go-live (existing check `email_scheduler_f_live_verify`) · 04 Stripe billing · 05 inbound reply-to-send. Checks: `email_scope_column`/`email_scoped_content`/`email_stripe_billing`/`email_inbound_reply`.
- [~] MCP co-build tools — `swfl_project_list/add/build` write tools; service-role write hard-bound to key's project (S9). Code COMPLETE locally (`9cdc302`, `eca9c7c`): per-project `mcp_key` mint/regenerate=revoke + "Connect your AI" UI; `X-Project-Key` header ONLY (no arg fallback — vendor-verified the SDK forwards the header to the tool `extra`; key is structurally absent from input schemas = never in tool-call logs); `[LB-R6b]` write hard-bound to the key's project (smuggled `project_id` ignored — negative test); `origin:'mcp'`; dedupe; shared `lib/deliverable/assemble.ts`. 28 MCP tests green, tsc/eslint clean. **HELD on two operator gates before ship: (a) `MCP_BEARER_TOKEN` SET in prod (keystone — server is unauthenticated until then), (b) live MCP-surface diff-review.** Live two-Claude verify + `mcp_project_tools_live_verify` close are post-deploy.
- [~] Pivoted Views execution (`docs/superpowers/plans/2026-06-12-pivoted-views-build/`) — wave 1+2 SHIPPED: §01 spec-fix; §02 `zhvi_pivoted`(316)/`zhvi_zip_latest`(109) + §06 `zori_pivoted`(136)/`zori_zip_latest`(94) views LIVE in prod (inert) + tested rollbacks; §03 `/charts`; §07 freshness per-view liveness; §04/§06 GATE A parity + equivalence harnesses (CI-skip without creds). **GATE A cycle 2/3 green** (ZHVI+ZORI 9/9, 2026-06-12 after nightly 10:15 UTC rebuild). ⬜ **cycle 3 → run 4 test files after Jun-13 nightly** → §05 cutover (home-values-swfl + rentals-swfl) unblocked → operator diff-review. ⬜ §08 `view_vintages` PARKED (separate greenlight).
