# Operation July — DONE

Finished Operation July tasks land here. When a task in `../Operation July/` is complete:

1. Flip its `Status` line to ✅ and note the live proof.
2. Close its check in the ledger (`node scripts/check.mjs close <key>`) if it has one.
3. `git mv "Operation July/<file>.md" "Operation-July-DONE/<file>.md"`.

`../Operation July/` should always show only what's still open. This folder is the archive of what
actually shipped (with the live proof recorded in each file).
