name: BLS LAUS monthly

on:
  schedule:
    # 12:00 UTC = 8 AM ET on the 4th of each month.
    # (Avoids clash with marketbeat-1st, corridor-narratives-2nd, county-pdfs-3rd.)
    - cron: "0 12 4 * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ingest:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install ingest dependencies
        run: pip install -r ingest/requirements.txt

      - name: Run bls_laus pipeline
        env:
          BLS_API_KEY: ${{ secrets.BLS_API_KEY }}
          BRAINS_SUPABASE_URL: ${{ secrets.BRAINS_SUPABASE_URL }}
          BRAINS_SUPABASE_SERVICE_KEY: ${{ secrets.BRAINS_SUPABASE_SERVICE_KEY }}
          SUPABASE_PG_HOST: ${{ secrets.SUPABASE_PG_HOST }}
          SUPABASE_PG_PORT: ${{ secrets.SUPABASE_PG_PORT }}
          SUPABASE_PG_DB: ${{ secrets.SUPABASE_PG_DB }}
          SUPABASE_PG_USER: ${{ secrets.SUPABASE_PG_USER }}
          SUPABASE_PG_PASSWORD: ${{ secrets.SUPABASE_PG_PASSWORD }}
        run: python -m ingest.pipelines.bls_laus.pipeline

      - name: Notify Slack on failure
        if: failure()
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          if [ -z "$SLACK_WEBHOOK_URL" ]; then
            echo "SLACK_WEBHOOK_URL not set — skipping Slack alert."
            exit 0
          fi
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          curl -sS -X POST -H "Content-Type: application/json" \
            -d "{\"text\": \":rotating_light: *bls-laus-monthly* failed.\\n• Run: ${RUN_URL}\"}" \
            "$SLACK_WEBHOOK_URL"
