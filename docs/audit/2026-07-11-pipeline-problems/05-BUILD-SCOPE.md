# 05 — BUILD SCOPE — RETIRED (absorbed into the consolidated build spec)

**Retired 07/11/2026.** The full Path-A architecture (the Spine, Phases 1–4, Act-today, and the unified sequencing) that lived here has been pulled into the single self-contained build spec:

➡️ **`docs/superpowers/specs/2026-07-11-data-contracts-doctor-design.md`**

That spec is now the one place to read *what to build and how it closes*. This file is a tombstone kept only so inbound references don't 404. The evidence trail is unchanged: `00-DIAGNOSIS.md` (the why) + `01`–`04` (the sweeps).
