# 2026-05-30 — Fixture-leak closed + speaker hygiene + corridor characters DONE + ops/ standalone + fgcu-reri + Goal 9

## Context

Covers 2026-05-27 through 2026-05-30. Last note was `2026-05-26.md` (MCP v1 live, PRs #19–#30). This period is 148 commits across ~8 sessions. Key arcs: corridor character generator closed, a fixture-mode leak in the live master payload patched properly, speaker layer cleaned up, ops/ moved to a standalone repo, new brains shipped, and the Goal 9 flywheel end-state documented.

Head at `3813c3b` on `main`.

---

## Decided

### Corridor character generator — CLOSED (Steps 0–5, PRs #40–#43)

- **PR #40** (`feat(corridor-character): Steps 2+3`) — fact pack + grounded pipeline + synthesizer + lint stack. Structured two-block design: `facts` block (cited, deterministic) + `character_speculative` block (AI-generated, exempt from no-smoothing rule per CLAUDE.md §8).
- **PR #41** (`chore(ingest): kill 3 dead broker-scrape pipelines`) — corridor, MarketBeat, and county-planning pipelines deleted. All three were landing pages returning no data; Firecrawl/Spider never produced rows. AI-generated voices cover what scraped broker narratives were supposed to deliver. `data_lake.marketbeat_swfl` remains empty; cre-swfl handles it gracefully.
- **PR #42** (`Step 4`) — 25/26 corridors on new structured output end-to-end.
- **PR #43** (`Step 4.5`) — type-conditional voice synthesis + chart-block audit tool. This supersedes the broker-scrape approach permanently.
- **Do not re-tighten the speculative block.** CLAUDE.md rule 8 carves it out explicitly.

### Fixture-leak in live master — FULLY CLOSED (PRs 1–3 per plan)

Live `/api/b/master` (v60/v61) was shipping fixture-mode text to users — "Fixture mode: only Lee County…", file paths, commit hashes, SCREAMING_SNAKE constants. Root cause: master lifts committed upstream `--- OUTPUT ---` blocks; `env-swfl`, `logistics-swfl`, and `traffic-swfl` were last rendered in fixture mode 10 days prior and never re-rendered live. The daily cron rebuilds master deterministically from those stale artifacts.

- **PR1 (manual rebuild):** Rebuilt LIVE — `env-swfl` v18, `logistics-swfl` v14, `logistics-swfl-nowcast` v12, `traffic-swfl` v9, then `master --target-only` → **v62 (`SWFL-7421-v62-20260530`)**. `data_lake.faf_flows` Postgres table never existed — logistics reads FAF5 **S3 Parquet via DuckDB** (3,430 flows). 56 orphaned `brains/*--{role}.md` role-view artifacts deleted (Stage-5 role-renderer is gone; corridor voices replaced roles).
- **Diff-review catch (not in plan):** Live FDOT `tfctr` is a PERCENT (range 0–92), not a fraction. `traffic-swfl` was multiplying by 100 → `truck_share_median = 739.6%`. Fixed at ingestion boundary in `refinery/sources/fdot-source.mts` (`fetchLive()` now ÷100). truck_share now **7.4%**. NOTE: `refinery/sources/fdot-freight-source.mts` (nowcast) independently treats `tfctr` as a percent and z-scores it — the ×100 cancels by design. Two FDOT source files now diverge on in-memory units; correct today, worth a future unify.
- **PR2 (gate):** New `refinery/lib/fixture-sentinels.mts` (`FIXTURE_SENTINELS` + `hasFixtureSentinel`). Stage 4 hard-fails before writing if rendered markdown carries a fixture sentinel in live mode. Self-correcting: master can't build until upstreams are re-rendered live. `logistics-swfl.mts` live build resolving 0 FAF5 flows now THROWS (mirrors `assertSegmentsNonEmpty`). Gate verified safe: `master --target-only --dry-run` passes on clean v62.
- **PR3 (speaker caveat hygiene):** `speaker.mts`: (A) pack-id regex no longer mangles compound paths; (B) `scrubCaveatTechnical` strips file paths→`[internal]`, commit hashes→`[ref]`, underscore constants→`[config]` (spares acronyms: SOFR/NFIP/FEMA/NAICS); (C) tier-2 caveats capped at 8 with "…and N more" tail; (D) fixture-sentinel backstop strips any leaked sentinel + prepends one honest line. `master.mts`: caveat reorder (cascade→floor→upstream). `cre-swfl.mts`: 25-area `MARKETBEAT_SUBMARKET_MAP` dump → `console.warn` + one clean coverage line.
- **Known follow-up (not blocking):** `cre-swfl` artifact-level caveat (PR3-D) needs an LLM-capable rebuild. It's the only pack without `skipSynthesisAgent`; hangs at stage 3 (`exit 124`, no LLM egress) in local/CI. Render-time scrub covers the live artifact meanwhile.

### Speaker language: corridor→area + NNN lock (commit `6457a3f`)

- New `deCorridor()` in `speaker.mts` — case-preserving `corridor(s)→area(s)`, word-boundary-safe (won't touch `corridor_type` data field). Applied to `sanitizeProse`, metric-table labels, and tier-2 scope header.
- NNN locked in two places: `rules-of-engagement.mts` rule 5 parenthetical + `cre-swfl.mts` synthesisContext hard line ("never expand to North Naples").
- Rules block (`rules-of-engagement.mts` + verbatim mirror in `docs/consumption-contract.md`): rule 3 rewritten (offer to pull what's missing; anti-fabrication preserved); new rule 6 "SPEAK IN PLACES" (metro/area altitude, zoom only when user names a spot). Block now **232/250 tokens** (~18 to spare, room for ~1 more terse rule). **Don't add rule 7 without trimming something first.**

### ops/ moved to standalone swfldatagulf-ops repo

- `ops/` directory deleted from this repo (commit `e442270`). Dashboard now lives at `swfldatagulf-ops` (separate repo). Deploy from there with `vercel --prod`.
- CLAUDE.md updated: all `ops/` references now point to the standalone repo. `docs/` reference index corrected.

### fgcu-reri brain — shipped, ops RED → GREEN (commit `e64b1cc`)

Full brain implementation. ops status moved from RED to GREEN.

### Goal 9 — compounding flywheel end-state documented

- `docs/THE-GOAL.md` + `docs/ontology-and-roadmap.md` updated with Goals 0-9 ladder. Goal 9 is the scored-history flywheel (falsifiable calls graded vs. observed outcomes → cohort-matched prediction by the 51st case). **Moat is the scored history, not the cited-facts layer.**
- `docs/sql/20260530_goal9_flywheel.sql` committed (insert-only seed). **Operator-gated — NOT yet run against production.** Await explicit instruction.
- Goals 0-8 live in `public.goals` Supabase table, seeded by `20260529_goals_table.sql` (run `2026-05-30`).

### master outputProducer confirmed LIVE — stale §6.1 flag killed

- `refinery/packs/master.mts:105` `masterSynthesizerOutputProducer` is fully implemented — pure-code deterministic synthesis, no LLM in the synthesis path. `skipSynthesisAgent` is true.
- **Pitch framing locked:** the no-invention guarantee is structural — the MCP/`/api/b/*` payload controls what context the model is handed; it can't fabricate what it was never given. "The system prevents it" ≫ "the AI won't."

### Data landmine — LeePA sale prices are 100% null

`data_lake.leepa_parcels` has sale DATES (119k rows) but `last_sale_amount` is **100% null** — ESRI field mis-map. No sale price available. Price needs a field-map fix, ATTOM, or FL DOR SDF. **Do not start any event-study or price-change work off this table.** Geometry is also Tier-1 only (geojson, join on FOLIOID — no geometry in Tier-2 Postgres).

### Other shipped items

- **Sector-credit-swfl:** `fl_dor_sales_tax` wired as additive second source (`86c52bb`).
- **Place→pocket resolver + payload gazetteer** (`051e678`): de-corridor display names, `place_name` in geo payload instead of internal corridor IDs.
- **Dynamic key_metrics cap** (`7a2e847`): `rollupKeyMetrics` now uses `t1Count+1`; flood-barrier-mode-1 caveat emits count + worst-case AAL (informative, not kill-switch).
- **FDOT PostgREST 1000-row cap bypassed** (PR #45): `fetchLive()` now paginates.
- **3 SQL migrations run 2026-05-29**: corridor rename (25 corridors), `fl_dor_sales_tax` `inserted_at`, `fgcu_reri_indicators` table (pipeline pending).
- **Firecrawl→Spider plain-scrape wrapper** (PR #48): `extract_client.scrape_with_fallback()` is now the canonical HTML-scrape path. Rule locked in `docs/standards/pipeline-freshness.md` + CLAUDE.md. Only `news_swfl/pipeline.py` was calling plain `scrape()` direct — migrated.
- **Census ACS pipeline spec** (`655e561`): full design doc at `docs/specs/` — tracts + ZCTAs, 8 covariates, Tier-2 DLT. Not yet executed.
- **PR #29 (permits-swfl v2) merged 2026-05-26**: pagination + per-permit detail fetch + `26TMP-*` filtering.
- **bls-laus cron fixed** (PR #48): moved to day 25 13:00 UTC (was day 4; BLS LAUS releases ~3rd-4th week of M+1).

---

## Open

- **PR #50 still OPEN** (`feat(mcp): add swfl HTTP transport to .mcp.json`, branch `claude/swfl-mcp-http-transport-u3uG7`). Auto-opened by workflow. 9 additions, 0 deletions. Operator has not merged it. Note: `c357f7c` already landed an `.mcp.json` swfl entry on main — check if PR #50 is now redundant before merging.
- **`news_swfl` — still not_yet_running.** Build queue checkbox still open. No consuming brain exists; pipeline is Tier-1 cold storage. Blocks until a consumer brain is scoped and the brain-first gate is satisfied.
- **cre-swfl artifact clean (PR3-D follow-up).** The `MARKETBEAT_SUBMARKET_MAP` constants in the live artifact will persist until the next LLM-capable rebuild. Render-time scrub covers it in the served payload. No code change needed — just needs a cron/manual rebuild with LLM egress.
- **Goal 9 flywheel SQL — NOT YET RUN.** `docs/sql/20260530_goal9_flywheel.sql` is committed but operator-gated. Wait for explicit go-ahead.
- **Census ACS pipeline — design only.** Spec is in docs; no ingest code written yet.
- **LeePA sale prices — null.** Root cause not fixed. Options: (1) fix ESRI field map in ingest pipeline, (2) ATTOM as price source, (3) FL DOR SDF. No PR open.
- **Vercel env-var rename still pending.** Code accepts both canonical (`SUPABASE_URL` / `SUPABASE_SERVICE_KEY`) and legacy (`BRAINS_SUPABASE_*`). Rename in Vercel dashboard → verify waitlist 200s → then remove the fallback in `app/api/waitlist/route.ts` (commit `c3b9d0a`).
- **`fdot-freight-source.mts` vs `fdot-source.mts` unit divergence.** Both treat `tfctr` as a percent in-memory but for different downstream reasons. A future unify PR would consolidate. Not urgent.
- **`fgcu_reri_indicators` pipeline.** Table exists (migration run `2026-05-29`); ingest pipeline not yet written.

---

## Verify next time

- `git log --oneline -5` — head should be `3813c3b docs: mark fixture-leak fix plan SHIPPED`.
- `curl -s https://www.swfldatagulf.com/api/b/master?view=speak&tier=2 | grep freshness_token` — should return `SWFL-7421-v62-20260530` (or later if a cron has since rebuilt). Should NOT contain "Fixture mode", "REFINERY_SOURCE", or any file path. If it does, the gate in `4-output.mts` or a stale upstream re-appeared.
- `cat ingest/cadence_registry.yaml | grep not_yet_running` — should list only `news_swfl` (down from 8 in the prior note as `fred_g17`, `bls_ppi`, `census_vip` have since fired).
- `gh pr list --state open` — should show PR #50 (`.mcp.json` swfl transport) only. Before merging, check if the `c357f7c` main commit makes it redundant.
- Before any LeePA price/value-change work: `SELECT COUNT(*), COUNT(last_sale_amount) FROM data_lake.leepa_parcels` — expect count ≈119k, price count = 0.
- Before running Goal 9 flywheel SQL: confirm operator gave explicit go-ahead. `docs/sql/20260530_goal9_flywheel.sql` is insert-only into `public.goals`.
- Before adding rule 7 to `rules-of-engagement.mts`: check token count. `rules-of-engagement.test.mts` hard-caps at 250; block is currently at ~232. Trim something first.
- `bun test` should report 810 pass / 0 fail (last confirmed after PR3 landed).
