# Recipes as config — build registration pointer

**The real spec is `2026-08-18-recipes-as-config-amendment-design.md`** (the amendment to the
08/02 one-lane plan). This stub exists only because `new-build.mjs` registers the build and
its check (`recipes_as_config_live_verify`) — do not write a second design here.

Operator decisions locked 08/18/2026: simplification GREENLIT ("Stop writing such dumb
fucking code and simplify all of this... It's 7 emails that are coded to create the same
things over and over"); quick-swap = **cycle button**, **paid**. Pace: the listing-lifecycle
emails collapse as a WAVE (the decree's target — they are the "same things over and over");
non-lifecycle recipes migrate per-touch after the layer is proven on the wave.
