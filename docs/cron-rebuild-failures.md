# Cron & Rebuild Failure Ledger

Operational incident log for brain-platform GHA workflows and the daily brain rebuild. One row per incident. **Newest-first.** Update `Status` in place when resolved; add new rows at the top.

Status key: `RESOLVED` | `OPEN` | `BLOCKED` | `FLAKE` (transient, retry-able, no code change)

---

| Date       | Workflow                                                                             | Error / Symptom                                                                | Root Cause                                                                                                                 | Status               | Fix / Action                                                                                                                         |
| ---------- | ------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| 2026-05-27 | `daily-rebuild`                                                                      | `FRED_API_KEY not set` — `macro-us-source` throws at startup                   | Key was in repo secrets but not wired into `daily-rebuild.yml` env block                                                   | RESOLVED             | commit `6ed974b`                                                                                                                     |
| 2026-05-26 | `daily-rebuild` #1 (first-ever schedule run)                                         | exit code 1, 17s total                                                         | Same as above — FRED key missing; exit was fast because macro-us-source fails early                                        | RESOLVED             | commit `6ed974b`                                                                                                                     |
| 2026-05-26 | 6 cold-lane Tier-1 workflows (USGS, HURDAT2, FHFA HPI, FDOT AADT, LEEPA Parcels + 1) | `KeyError: 'SUPABASE_S3_ENDPOINT'` — blocked at env read before any data fetch | `SUPABASE_S3_ENDPOINT` / `SUPABASE_S3_ACCESS_KEY_ID` / `SUPABASE_S3_SECRET_ACCESS_KEY` absent from repo secrets            | RESOLVED             | PR #31 — secrets added + exposed in workflow `env:` blocks                                                                           |
| 2026-05-26 | `redfin-monthly`                                                                     | `Error: actions/checkout@v6` — action version nonexistent                      | Workflow authored with `checkout@v6` + `setup-python@v6`; those versions do not exist                                      | RESOLVED             | commit `83a571f` — bumped to `checkout@v4` + `setup-python@v5`                                                                       |
| 2026-05-26 | `lee-permits-weekly`                                                                 | `ModuleNotFoundError: No module named 'bs4'`                                   | `beautifulsoup4` not pinned in workflow Python deps                                                                        | RESOLVED             | PR #31                                                                                                                               |
| 2026-05-26 | `faf5-annual`                                                                        | `relation "data_lake.faf_sctg_lookup" does not exist`                          | DLT state drift — `dlt_pipeline_state` entry exists so DLT skips CREATE TABLE, crashes on INSERT                           | OPEN                 | Needs versioned DDL file (`docs/sql/YYYYMMDD_faf_sctg_lookup.sql`); do not use ad-hoc CREATE TABLE                                   |
| 2026-05-26 | `marketbeat-quarterly`                                                               | Firecrawl returns `{"success": true, "id": "..."}` — 0 rows loaded             | Pipeline treated immediate async response as final data; no polling loop for job completion                                | RESOLVED             | Pipeline deleted PR #41 (landing page only, no data surface)                                                                         |
| 2026-05-26 | `corridor-narratives-quarterly`                                                      | 0 rows from all 4 broker URLs                                                  | CRE Consultants rebuilt site March 2026 (no public research surface); IPC rebranded/moved; SVN + LSI still functional      | RESOLVED             | Pipeline deleted PR #41; broker-replacement work tracked in `docs/superpowers/plans/2026-05-26-corridor-broker-narrative-promotion/` |
| 2026-05-26 | `county-planning-monthly`                                                            | 0 decisions returned for Lee + Collier                                         | `COUNTY_CONFIGS` URLs stale — county planning portals reshuffled                                                           | RESOLVED             | Pipeline deleted PR #41                                                                                                              |
| 2026-05-26 | `fema-nfip-quarterly`                                                                | `SSL: UNEXPECTED_EOF_WHILE_READING` at `resultOffset=4000`                     | FEMA `hazards.fema.gov` dropped connection mid-pagination                                                                  | FLAKE                | Retry twice before treating as structural; no code change needed                                                                     |
| 2026-05-26 | `census-vip-monthly`                                                                 | `ReadTimeout` (60s) from `api.census.gov`                                      | Census API throttle on first-ever run                                                                                      | RESOLVED (transient) | Passed on retry                                                                                                                      |
| 2026-05-26 | `census-cbp-annual`                                                                  | Timeout on first dispatch                                                      | Transient Census API timeout                                                                                               | RESOLVED (transient) | Confirmed success on re-run                                                                                                          |
| 2026-05-25 | `lee-permits-weekly` (scraper)                                                       | 0 rows — scraper hit wrong host + wrong form selectors                         | 4 inherited code bugs: wrong Accela host, wrong module path, dead shape, wrong form IDs (`txtFrmDate` vs `txtGSStartDate`) | RESOLVED             | commit `e78b17b` — fixes + `firecrawl-py 4.28+` pinned (PR #13)                                                                      |

---

## Pre-flight: Known Pending Failures

These will fail on their next scheduled run unless fixed first.

| Workflow      | Expected Failure                                      | Required Fix Before Run                                                                                             |
| ------------- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| `faf5-annual` | `relation "data_lake.faf_sctg_lookup" does not exist` | Write `docs/sql/YYYYMMDD_faf_sctg_lookup.sql` as a versioned DDL; clear DLT pipeline state for `faf5` before re-run |

---

## Pre-flight: First-Fire Pending

Workflows that have shipped but have never run in production. Not failures yet — monitor on first fire.

| Workflow                  | Pipeline                            | First Fire                            | Notes                                                                                                                                                                                                                           |
| ------------------------- | ----------------------------------- | ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `collier-permits-monthly` | `ingest/pipelines/collier_permits/` | 5th of next month (cron `0 12 5 * *`) | Dry-run: 5,030 rows April 2026 XLSX, 35/35 tests pass. SQL migration: `docs/sql/20260527_collier_building_permits.sql`. Move from `not_yet_running:` → `pipelines:` in `cadence_registry.yaml` after first successful live run. |
