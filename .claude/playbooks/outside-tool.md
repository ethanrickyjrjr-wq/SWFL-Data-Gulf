---
name: outside-tool
for: "how do we get X", "can this help us", a link to a repo / vendor / model / plugin — evaluate something from outside
---
### Outside tool evaluation

**You own the verdict, and the verdict is usually no.** Every recent "make the AI better" lead ended do-not-adopt; the leverage is finishing and deleting, not adding.

1. `_RESEARCH/INDEX.md` for the tool and its shape (Writ, Omnigent, block/buzz, pstack, dbt, DuckLake, RentCast, MinerU are already evaluated). A prior verdict is answered, not re-litigated.
2. crawl4ai the LIVE source — README, manifest, license, install path — never Firecrawl, never memory (RULE 0.4). Pinned interpreter `C:\Users\ethan\crawl4ai-venv\Scripts\python.exe`.
3. Name what it actually is: install surface (which harness), the load-bearing feature, license, whether the load-bearing feature is reachable from our stack at all.
4. Map every non-vendor piece onto what we already run (superpowers, `verify`, `second-order`, `what-do-we-have`, hooks, checks). Overlap is the default finding.
5. Apply the freeze: NORTH STAR #5 — adopt nothing new for 30 days from 08/19/2026 — is in force unless the operator lifts it in words. Never rebut a strategic direction with a cost table (RULE 0.9).
6. RULE 11: does the pattern justify itself at OUR volume? Hyperscaler patterns are dropped.
7. Write the evaluation to `_RESEARCH/<category>/YYYY-MM-DD-<slug>-evaluation.md` with source URL + date, and its INDEX.md line, in the same pass. Unindexed research does not exist.
8. Verdict shape: DO NOT ADOPT / ADOPT / STEAL THE SHAPE — name the one thing worth stealing and which standing priority it serves, or say "nothing to steal".

**Rules carried:** RULE 0.4 (crawl4ai, file + index) · RULE 0.9 · RULE 11 · NORTH STAR #5 · crawl4ai-only memories.

**Reply:** what it is, whether it even runs here, the overlap map, the verdict, the file path filed. Plain text, no tables.
