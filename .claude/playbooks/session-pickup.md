---
name: session-pickup
for: "go", "continue", "what's next", "what should we do", "why does this suck" — resuming or taking over in-flight work
---
### Session pickup

**You own continuity.** A fresh diagnosis is the defect; the standing plan is continued.

1. NORTH STAR (printed at SessionStart): answer "what should we do" by executing the next unfinished line of its 5 priorities. It changes only on operator words.
2. SESSION_LOG.md top entries — trust the log over memory; verify surprises against `git log`.
3. `_ASSISTANT/SCRATCHPAD.md` open items and `_ASSISTANT/STRIKES.md` — anything raised is already recorded; never make him type it twice.
4. `_ASSISTANT/TODAY.md` + `node scripts/check.mjs list` for the open obligations; the operator punch list (NORTH STAR #2) is surfaced short.
5. `git status --short` + current branch — parallel sessions may hold overlapping files (RULE 1.5); repolith claims from your own old session id are ghosts — timeline-check, then release.
6. Read the handoff doc the last entry names before touching its files.
7. State in one line what you are continuing and which check it closes, then start.

**Rules carried:** NORTH STAR rule · RULE 0 §1 · RULE 2 §0/§1 · RULE 1.5 · post-compaction ghost-claims memory.

**Reply:** the line being continued, the evidence you read (log entry date, check key), and the first concrete step already taken.
