[normalize.data_writer]
file_max_items    = 500
buffer_max_items  = 500

[load]
workers = 1

[pipeline.faf5]
pipeline_name = "faf5"
dataset_name  = "data_lake"

[pipeline.census_cbp]
pipeline_name = "census_cbp"
dataset_name  = "data_lake"

[pipeline.tier1_inventory]
pipeline_name = "tier1_inventory"
dataset_name  = "data_lake"

# [pipeline.usgs]  -- DEPRECATED: replaced by ingest/duckdb_pipelines/usgs/pipeline.py
# pipeline_name = "usgs"
# dataset_name  = "data_lake"
