dlt[postgres]>=1.26.0
requests>=2.31.0
pytest>=8.0.0
python-dotenv>=1.0.0
duckdb>=1.1
pandas>=2.0.0
pyarrow>=14.0
anthropic>=0.49.0
# crawl4ai is the ONLY web-crawl/scrape tool (operator decree 2026-06-16). It drives the
# Accela permit portals (lee_permits/collier_permits) via UndetectedAdapter and every
# fetch_page_*/fetch_many caller — the Firecrawl/Spider replacement for static AND
# interactive/stealth scraping. GHA needs a `crawl4ai-setup` / `playwright install chromium`
# step + a non-datacenter IP (deferred with the GHA-IP follow-on; local-first for now).
# Pinned EXACT to the proven, battle-tested 0.8.9 — `>=` let a fresh install pull an
# unverified 0.9.x (in-process SDK is unchanged at 0.9.0 per docs, but verify-then-bump).
crawl4ai==0.8.9
# firecrawl-py is intentionally NOT a dependency: Firecrawl is removed per the decree.
# ingest/lib/firecrawl_client.py + the extract_client fallback branch are dormant code,
# gated on FIRECRAWL_API_KEY (unset) and a lazy `from firecrawl import Firecrawl` that
# raises a caught error if absent — they never fire. Re-add this pin ONLY if the deferred
# crawl4ai-native extract() rewire decides to keep Firecrawl as a paid last resort.
psycopg[binary]>=3.2
pyyaml>=6.0
beautifulsoup4>=4.12
openpyxl>=3.1
python-dateutil>=2.9
pdfplumber>=0.10
pymupdf>=1.23
